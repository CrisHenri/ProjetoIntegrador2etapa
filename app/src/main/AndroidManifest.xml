<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.cnec.fcsl.projetointegrador">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Deputado" />
        <activity android:name=".DeputadosPartido" />
        <activity android:name=".ListaDePartidos" />
        <activity android:name=".ListaDeTodosDeputados" />
        <activity android:name=".TelaInicial" />
        <activity android:name=".InfoDeputadoActivity"></activity>
    </application>

</manifest>